[project]
name = "kphtools"
version = "0.1.0"
description = "Toolkits for KPH dynamic data"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "alibabacloud-oss-v2",
    "anthropic",
    "httpx",
    "mcp",
    "openai",
    "oss2>=2.19.1",
    "pefile",
    "pyyaml",
    "requests",
    "signify",
    "watchdog>=6.0.0",
]

[tool.uv]
package = false
