# find-ExDereferenceCallBackBlock

Find kernel function `ExDereferenceCallBackBlock` in the current IDA database and write `ExDereferenceCallBackBlock.yaml` with:

- `category: func`
- `func_name`
- `func_rva`
