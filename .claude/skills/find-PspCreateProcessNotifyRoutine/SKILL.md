# find-PspCreateProcessNotifyRoutine

Find kernel global variable `PspCreateProcessNotifyRoutine` in the current IDA database and write `PspCreateProcessNotifyRoutine.yaml` with:

- `category: gv`
- `gv_name`
- `gv_rva`
