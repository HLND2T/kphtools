# find-PspCreateThreadNotifyRoutine

Find kernel global variable `PspCreateThreadNotifyRoutine` in the current IDA database and write `PspCreateThreadNotifyRoutine.yaml` with:

- `category: gv`
- `gv_name`
- `gv_rva`
