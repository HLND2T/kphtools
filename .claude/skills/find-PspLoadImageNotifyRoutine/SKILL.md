# find-PspLoadImageNotifyRoutine

Find kernel global variable `PspLoadImageNotifyRoutine` in the current IDA database and write `PspLoadImageNotifyRoutine.yaml` with:

- `category: gv`
- `gv_name`
- `gv_rva`
